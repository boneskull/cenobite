// foo module - depends on bar
import { barNumber, barObject, barValue } from 'bar';

export const fooValue = `Foo says: ${barValue}`;
export const fooNumber = barNumber * 2;
export const fooObject = {
  ...barObject,
  message: 'Foo module loaded successfully',
  compartment: 'foo-compartment',
  fromBar: barObject
};

// Re-export everything from bar
export { barNumber, barObject, barValue } from 'bar';

export default {
  fooValue,
  fooNumber,
  fooObject,
  barValue,
  barNumber,
  barObject
};
